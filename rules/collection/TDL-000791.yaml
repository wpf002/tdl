rule_id: TDL-COL-000791
v4_id: 000791
name: Mailbox Auto-Forward Rule Created to External Domain
description: New-InboxRule / message-rule creation with ForwardTo / RedirectTo to a non-corporate domain. Classic mailbox
  theft pattern. Generated from gap analysis against canonical ATT&CK Enterprise matrix (T1114 under Collection).
tactic: Collection
tactic_id: TA0009
technique_id: T1114
technique_name: Email Collection
platform:
- Microsoft 365
- Google Workspace
data_sources:
- SaaS Audit Logs
severity: High
fidelity: Medium
lifecycle: Proposed
queries:
  spl: 'index=network sourcetype=firewall OR sourcetype=ids

    | search rule.action="*auto-forward*"

    | stats count, dc(dest_ip) as unique_dests, sum(bytes_out) as bytes_out by src_ip, dest_ip, dest_port

    | where count >= 1

    | sort - bytes_out'
  kql: 'union (DeviceNetworkEvents), (CommonSecurityLog | extend SourceIP=tostring(SourceIP), DestinationIP=tostring(DestinationIP))

    | where TimeGenerated > ago(1d)

    | where rule.action matches regex "auto-forward"

    | summarize Count=count(), Bytes=sum(coalesce(SentBytes, 0)) by SourceIP, DestinationIP, DestinationPort

    | order by Bytes desc'
  aql: 'SELECT sourceip, destinationip, destinationport, SUM(eventcount) as connections, SUM(bytessrc) as bytes_out

    FROM flows

    WHERE "rule.action" LIKE ''%auto-forward%''

    GROUP BY sourceip, destinationip, destinationport

    LAST 1 HOURS'
  yara_l: "rule tdl_tdl_col_000791 {\n  meta:\n    author = \"TDL\"\n    description = \"Mailbox Auto-Forward Rule Created\
    \ to External Domain\"\n    severity = \"HIGH\"\n    tactic = \"TA0009\"\n    technique = \"T1114\"\n  events:\n    $e.metadata.event_type\
    \ = \"NETWORK_CONNECTION\"\n    $e.rule.action = /auto-forward/\n    $src = $e.principal.ip\n  match:\n    $src over 10m\n\
    \  condition:\n    $e\n}"
  esql: 'FROM logs-network*,logs-endpoint*

    | WHERE @timestamp > NOW() - 1 hour

    | WHERE event.category == "network" AND rule.action LIKE "%auto-forward%"

    | STATS bytes_out = SUM(network.bytes), conns = COUNT(*) BY source.ip, destination.ip, destination.port

    | SORT bytes_out DESC'
  leql: 'where(logset = "Network Activity" AND rule.action=/auto-forward/)

    groupby(source_ip, destination_ip, destination_port)

    calculate(count, sum(bytes_out))'
  crowdstrike: 'event_simpleName IN (NetworkConnectIP4, NetworkConnectIP6, DnsRequest) AND rule.action=*auto-forward*

    | stats count as conns, sum(BytesSent) as bytes_out by ComputerName, RemoteAddressIP4, RemotePort

    | sort -bytes_out'
  xql: 'dataset = xdr_data

    | filter event_type = ENUM.NETWORK AND rule.action ~= "auto-forward"

    | comp count() as conns, sum(action_total_bytes) as bytes_out by agent_hostname, action_remote_ip, action_remote_port

    | sort desc bytes_out'
  lucene: event.category:"network" AND rule.action:*auto-forward*
pseudo_logic: New-InboxRule / message-rule creation with ForwardTo / RedirectTo to a non-corporate domain. Classic mailbox
  theft pattern.
false_positives:
- Sanctioned admin or engineering activity
- Approved automation / CI workflows
tuning_guidance: "Proposed rule generated from ATT&CK gap analysis \u2014 review the pseudo_logic, scope filters to the right\
  \ asset group, and refine thresholds using \u22657 days of telemetry before promoting."
tuning_period: 14 days
tags:
- email
- mailbox
- t1114
- collection
references:
- https://attack.mitre.org/techniques/T1114/
author: TDL gap-analysis generator
created: '2026-04-27'
last_modified: '2026-04-27'
test_method: none

rule_id: TDL-DE-000185
v4_id: 000185
name: Code Compiling
description: Detects an account using a compiler on a machine. Attackers can download or develop source code on a host to
  bypass detection of executable files and then compile it locally on the host to avoid detection.
tactic: Defense Evasion
tactic_id: TA0005
technique_id: T1027
technique_name: Obfuscated Files or Information
platform:
- Linux
- Windows
data_sources:
- Windows Operating System
- Linux Operating System
- 'Event IDs: 4688'
severity: Low
fidelity: Low
lifecycle: Proposed
risk_score: 30
queries:
  spl: |
    index=endpoint sourcetype=Sysmon EventCode=1
    | search event_id IN ("4688")
    | stats count, values(host) as hosts, values(user) as users by process_name, parent_process_name, command_line
    | where count >= 1
    | sort - count
  kql: |
    DeviceProcessEvents
    | where TimeGenerated > ago(1d)
    | where event_id in ("4688")
    | summarize Count=count() by DeviceName, AccountName, FileName, InitiatingProcessFileName, ProcessCommandLine
    | order by Count desc
  aql: |
    SELECT DEVICETIME, sourceip, "Process Name", "Parent Process Name", "Process CommandLine"
    FROM events
    WHERE LOGSOURCETYPENAME(devicetype) = 'Microsoft Windows Sysmon'
      AND "EventID" = '1'
      AND "event_id" IN ('4688')
    LAST 24 HOURS
  yara_l: |
    rule tdl_tdl_de_000185 {
      meta:
        author = "TDL"
        description = "Code Compiling"
        severity = "LOW"
        tactic = "TA0005"
        technique = "T1027"
      events:
        $e.metadata.event_type = "PROCESS_LAUNCH"
        $e.event_id = /(4688)/
        $host = $e.principal.hostname
      match:
        $host over 30m
      condition:
        $e
    }
  esql: |
    FROM logs-endpoint*,logs-windows*
    | WHERE @timestamp > NOW() - 1 hour
    | WHERE event.category == "process" AND event_id IN ("4688")
    | STATS count = COUNT(*) BY host.name, user.name, process.name, process.parent.name, process.command_line
    | SORT count DESC
  leql: |
    where(logset = "Endpoint Activity" AND event_id IN [4688])
    groupby(asset, process_name, parent_process_name)
    calculate(count)
  crowdstrike: |
    event_simpleName=ProcessRollup2 AND event_id IN (4688)
    | stats count as occurrences by ComputerName, UserName, FileName, ParentBaseFileName, CommandLine
    | sort -occurrences
  xql: |
    dataset = xdr_data
    | filter event_type = ENUM.PROCESS AND event_id in ("4688")
    | comp count() as occurrences by agent_hostname, actor_effective_username, action_process_image_name, causality_actor_process_image_name, action_process_image_command_line
    | sort desc occurrences
  lucene: |
    event.category:"process" AND event_id:(4688)
  sumo: |
    _sourceCategory=*sysmon* OR _sourceCategory=*endpoint*
    | parse "EventID=*\"" as event_id
    | where event_id="1" AND event_id IN ("4688")
    | count by host, user, parent_process_name, command_line
    | sort by _count desc
false_positives:
- 'Old Regex for the command line: \b(gcc|g\+\+|cc|c\+\+|javac)\b


  ----------------------------------------------------------------


  New Regex matches on'
tuning_guidance: 'Tune per environment baseline. Tuning period: 7 days.'
tags:
- edp
- defense-evasion
author: TDL
created: '2024-01-01'
last_modified: '2026-04-28'
test_method: none
pseudo_logic: "1 Windows Process Execution Event \nWhere the Event ID is in [592, 4688]\nWhere the Destination Hostname is\
  \ NOT on a High Value Target Host List\nWhere the Command matches the regex \"(?:(?:^[\\\"\\s]{0,1}(gcc|g\\+\\+|cc|c\\+\\\
  +|javac)\\b)|(?:^(?:[\\\\\\/](?:\\w|\\\\\\s)*)+(gcc|g\\+\\+|cc|c\\+\\+|javac)\\b)|(?:\\|\\s*(gcc|g\\+\\+|cc|c\\+\\+|javac)\\\
  b)|(?:^[\\\"\\'][^\\\"]*[\\\\\\/](gcc|g\\+\\+|cc|c\\+\\+|javac)[\\\"\\']))\"\n\n---------------------------------------------------------------------------------------------------------------\n\
  \n1 Linux Command Line Event\nWhere the Destination Hostname is NOT on a High Value Target Host List\nWhere the Command\
  \ matches the regex \"(?:(?:^[\\\"\\s]{0,1}(gcc|g\\+\\+|cc|c\\+\\+|javac)\\b)|(?:^(?:[\\\\\\/](?:\\w|\\\\\\s)*)+(gcc|g\\\
  +\\+|cc|c\\+\\+|javac)\\b)|(?:\\|\\s*(gcc|g\\+\\+|cc|c\\+\\+|javac)\\b)|(?:^[\\\"\\'][^\\\"]*[\\\\\\/](gcc|g\\+\\+|cc|c\\\
  +\\+|javac)[\\\"\\']))\""
suppression:
  window: 24 hours
  fields:
  - Source Username
tuning_period: 7 days
triage_steps:
- 'Confirm the alert metadata: timestamp, host, user, and the evasion artifact (process / DLL / log clear / service stop).'
- Inspect the parent → child process tree, command line arguments, and code-signing status — distinguish administrative tooling
  from adversary tradecraft (LOLBins, signed binary proxies, masquerading paths).
- 'Pull surrounding security-tool telemetry: EDR sensor health, AV scan history, log channel state to see what was suppressed.'
- 'Look for adjacent stages: a successful evasion typically wraps an execution / persistence / cred-access action in the same
  minute.'
- 'Decide TP / FP: FP if patch / debugging / IT-troubleshooting context fully accounts for it; TP if it''s unattended and
  pairs with other malicious telemetry.'
- 'If TP: restore the disabled defenses, capture artifacts before evidence rolls out of retention, isolate the host, and escalate
  to IR.'

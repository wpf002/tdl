rule_id: TDL-DIS-000761
v4_id: '000761'
name: gpresult / Get-GPO Discovery from Workstation
description: gpresult.exe /h or Get-GPO / Get-GPOReport invoked outside admin desktops. Pre-attack GPO reconnaissance. Generated
  from gap analysis against canonical ATT&CK Enterprise matrix (T1615 under Discovery).
tactic: Discovery
tactic_id: TA0007
technique_id: T1615
technique_name: Group Policy Discovery
platform:
- Windows
data_sources:
- Sysmon
severity: Medium
fidelity: Medium
lifecycle: Proposed
queries:
  spl: 'index=endpoint sourcetype=Sysmon EventCode=1

    | search process.command_line="*gpresult*"

    | stats count, values(host) as hosts, values(user) as users by process_name, parent_process_name, command_line

    | where count >= 1

    | sort - count'
  kql: 'DeviceProcessEvents

    | where TimeGenerated > ago(1d)

    | where process.command_line matches regex "gpresult"

    | summarize Count=count() by DeviceName, AccountName, FileName, InitiatingProcessFileName, ProcessCommandLine

    | order by Count desc'
  aql: "SELECT DEVICETIME, sourceip, \"Process Name\", \"Parent Process Name\", \"Process CommandLine\"\nFROM events\nWHERE\
    \ LOGSOURCETYPENAME(devicetype) = 'Microsoft Windows Sysmon'\n  AND \"EventID\" = '1'\n  AND \"process.command_line\"\
    \ LIKE '%gpresult%'\nLAST 24 HOURS"
  yara_l: "rule tdl_tdl_dis_000761 {\n  meta:\n    author = \"TDL\"\n    description = \"gpresult / Get-GPO Discovery from\
    \ Workstation\"\n    severity = \"MEDIUM\"\n    tactic = \"TA0007\"\n    technique = \"T1615\"\n  events:\n    $e.metadata.event_type\
    \ = \"PROCESS_LAUNCH\"\n    $e.process.command_line = /gpresult/\n    $host = $e.principal.hostname\n  match:\n    $host\
    \ over 30m\n  condition:\n    $e\n}"
  esql: 'FROM logs-endpoint*,logs-windows*

    | WHERE @timestamp > NOW() - 1 hour

    | WHERE event.category == "process" AND process.command_line LIKE "%gpresult%"

    | STATS count = COUNT(*) BY host.name, user.name, process.name, process.parent.name, process.command_line

    | SORT count DESC'
  leql: 'where(logset = "Endpoint Activity" AND process.command_line=/gpresult/)

    groupby(asset, process_name, parent_process_name)

    calculate(count)'
  crowdstrike: 'event_simpleName=ProcessRollup2 AND process.command_line=*gpresult*

    | stats count as occurrences by ComputerName, UserName, FileName, ParentBaseFileName, CommandLine

    | sort -occurrences'
  xql: 'dataset = xdr_data

    | filter event_type = ENUM.PROCESS AND process.command_line ~= "gpresult"

    | comp count() as occurrences by agent_hostname, actor_effective_username, action_process_image_name, causality_actor_process_image_name,
    action_process_image_command_line

    | sort desc occurrences'
  lucene: event.category:"process" AND process.command_line:*gpresult*
pseudo_logic: gpresult.exe /h or Get-GPO / Get-GPOReport invoked outside admin desktops. Pre-attack GPO reconnaissance.
false_positives:
- Sanctioned admin or engineering activity
- Approved automation / CI workflows
tuning_guidance: "Proposed rule generated from ATT&CK gap analysis \u2014 review the pseudo_logic, scope filters to the right\
  \ asset group, and refine thresholds using \u22657 days of telemetry before promoting."
tuning_period: 14 days
tags:
- gpo
- enum
- t1615
- discovery
references:
- https://attack.mitre.org/techniques/T1615/
author: TDL gap-analysis generator
created: '2026-04-27'
last_modified: '2026-04-27'
test_method: none

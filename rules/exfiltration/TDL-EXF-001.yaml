rule_id: TDL-EXF-001
name: Large Data Transfer to External Cloud Storage
tactic: Exfiltration
tactic_id: TA0010
technique_id: T1567.002
technique_name: Exfiltration to Cloud Storage
platform:
- Windows
- Linux
- macOS
data_sources:
- Proxy/web gateway logs
- Firewall logs
severity: High
fidelity: Medium
lifecycle: Deployed
risk_score: 80
queries:
  spl: 'index=network sourcetype=proxy

    | where match(dest_host, "(?i)(mega\.nz|dropbox\.com|box\.com|wetransfer\.com)")

    | bucket _time span=1h

    | stats sum(bytes_out) as total_bytes by src_ip, dest_host, _time

    | where total_bytes >= 104857600

    | eval mb=round(total_bytes/1048576,2)

    | table _time, src_ip, dest_host, mb'
  kql: 'CommonSecurityLog

    | where TimeGenerated >= ago(1d)

    | where DestinationHostName matches regex @"(?i)(mega\.nz|dropbox\.com|box\.com|wetransfer\.com)"

    | summarize TotalBytes = sum(SentBytes) by SourceIP, DestinationHostName, bin(TimeGenerated, 1h)

    | where TotalBytes >= 104857600

    | extend MBOut = round(TotalBytes/1048576.0, 2)'
  aql: |-
    SELECT sourceip, destinationip, destinationport, SUM(flowCount) AS total_flows
    FROM flows
    WHERE INOFFSET(starttime, LAST 300 SECONDS)
    GROUP BY sourceip, destinationip, destinationport
    ORDER BY starttime DESC
  yara_l: |-
    rule tdl_exf_001 {
      meta:
        author = "TDL"
        description = "Large Data Transfer to External Cloud Storage"
        severity = "HIGH"
        tactic = "TA0010"
        technique = "T1567.002"

      events:
        $event.metadata.event_type = "GENERIC_EVENT"
        $event.principal.hostname = $host
        $event.principal.user.userid = $user

      match:
        $host over 5m

      condition:
        #event >= 1

      outcome:
        $risk_score = max(50)
    }
  esql: |-
    FROM logs-network*,logs-firewall*
    | WHERE @timestamp > NOW() - 1 day
    | STATS event_count = COUNT(*) BY host.name, user.name, @timestamp = BUCKET(@timestamp, 5m)
    | WHERE event_count >= 1
    | SORT @timestamp DESC
  leql: |-
    where(logset = "Endpoint Activity")
    groupby(hostname, username)
    calculate(count AS event_count)
    having(event_count >= 1)
  crowdstrike: |-
    event_simpleName=ProcessRollup2
    | stats count as proc_count by ComputerName, FileName
    | where proc_count >= 1
    | sort -proc_count
  xql: "dataset = xdr_data\n| filter event_type = \"NETWORK\" \n| filter _time > to_timestamp(now() - 86400, \"epoch\")\n\
    | fields hostname, actor_effective_username, action_process_image_name, action_process_command_line, _time\n| sort desc\
    \ _time"
  lucene: |-
    event_id:* AND log_type:"security"
false_positives:
- Approved cloud storage use
- IT file sharing
tags:
- exfiltration
- cloud-storage
- dlp
author: TDL
created: '2024-03-15'
test_method: historical
last_modified: '2026-04-27'

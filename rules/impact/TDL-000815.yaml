rule_id: TDL-IMP-000815
v4_id: 000815
name: Modification of Financial / Audit Records by Non-App Identity
description: UPDATE/DELETE on financial or audit tables by a database account outside the application service account allowlist. Generated from
  gap analysis against canonical ATT&CK Enterprise matrix (T1565 under Impact).
tactic: Impact
tactic_id: TA0040
technique_id: T1565
technique_name: Data Manipulation
platform:
- Windows
- Linux
- AWS
data_sources:
- Auditd
- Sysmon
- Cloud Audit Logs
severity: High
fidelity: Medium
lifecycle: Proposed
risk_score: 75
queries:
  spl: "index=* (sourcetype=linux_secure OR sourcetype=auditd OR sourcetype=\"WinEventLog:Microsoft-Windows-Sysmon/Operational\" OR sourcetype=aws:cloudtrail\
    \ OR sourcetype=aws:rds:audit) \n(action=UPDATE OR action=DELETE OR comm=UPDATE OR comm=DELETE OR query=*UPDATE* OR query=*DELETE* OR statement=*UPDATE*\
    \ OR statement=*DELETE*)\n(table=*financial* OR table=*audit* OR table=*transaction* OR table=*payment* OR object=*financial* OR object=*audit*\
    \ OR object=*transaction* OR object=*payment* OR query=*financial* OR query=*audit* OR query=*transaction* OR query=*payment*)\nNOT (user=app_svc\
    \ OR user=application_service OR user=svc_app OR user=db_app OR user=service_account OR account_name=app_svc OR account_name=application_service\
    \ OR account_name=svc_app OR account_name=db_app OR account_name=service_account)\n| eval operation=coalesce(action, comm, \"QUERY\")\n| eval\
    \ affected_table=coalesce(table, object, \"UNKNOWN\")\n| eval db_user=coalesce(user, account_name, User, SourceUser, userIdentity.userName)\n\
    | where (match(operation, \"(?i)UPDATE\") OR match(operation, \"(?i)DELETE\"))\n| stats count earliest(_time) as first_time latest(_time)\
    \ as last_time values(affected_table) as tables values(operation) as operations values(host) as hosts by db_user\n| where count > 0\n| eval\
    \ first_time=strftime(first_time, \"%Y-%m-%d %H:%M:%S\"), last_time=strftime(last_time, \"%Y-%m-%d %H:%M:%S\")\n| table db_user, count, operations,\
    \ tables, hosts, first_time, last_time\n"
  kql: 'union AWSCloudTrail, AzureActivity, AuditLogs

    | where TimeGenerated > ago(1d)

    | where eventName matches regex ".+"

    | summarize Count=count() by Identity, EventName=coalesce(EventName, OperationName), Resource=tostring(coalesce(ResourceId, ResourceProvider))

    | order by Count desc

    '
  aql: "SELECT DEVICETIME, \"User Identity\", \"Event Name\", \"Source IP Address\"\nFROM events\nWHERE LOGSOURCETYPENAME(devicetype) IN ('AWS\
    \ CloudTrail','Azure Audit','GCP Cloud Audit')\n  AND \"eventName\" LIKE '%.+%'\nLAST 24 HOURS\n"
  yara_l: "rule tdl_tdl_imp_000815 {\n  meta:\n    author = \"TDL\"\n    description = \"Modification of Financial / Audit Records by Non-App\
    \ Identity\"\n    severity = \"HIGH\"\n    tactic = \"TA0040\"\n    technique = \"T1565\"\n  events:\n    $e.metadata.event_type = \"USER_RESOURCE_ACCESS\"\
    \n    $e.eventName = /.+/\n    $actor = $e.principal.user.userid\n  match:\n    $actor over 30m\n  condition:\n    $e\n}\n"
  esql: 'FROM logs-aws*,logs-azure*,logs-gcp*

    | WHERE @timestamp > NOW() - 1 hour

    | WHERE eventName LIKE "%.+%"

    | STATS count = COUNT(*) BY user.name, event.action, cloud.account.id

    | SORT count DESC

    '
  leql: 'where(logset IN ["AWS CloudTrail", "Azure Audit", "GCP Audit"] AND eventName=/.+/)

    groupby(actor, event_name)

    calculate(count)

    '
  crowdstrike: 'event_simpleName=CloudOcsfApiActivity AND eventName=*.+*

    | stats count as calls by Actor, ApiName, SourceIPAddress

    | sort -calls

    '
  xql: 'dataset = cloud_audit_logs

    | filter eventName ~= ".+"

    | comp count() as calls by actor_effective_username, action_evtlog_event_name, action_remote_ip

    | sort desc calls

    '
  lucene: 'event.dataset:("aws.cloudtrail" OR "azure.audit" OR "gcp.audit") AND eventName:*.+*

    '
  sumo: '_sourceCategory=*aws/cloudtrail* OR _sourceCategory=*azure/audit* OR _sourceCategory=*gcp/audit*

    | where eventName matches "*.+*"

    | count by userIdentity_arn, eventName, sourceIPAddress

    | sort by _count desc

    '
pseudo_logic: UPDATE/DELETE on financial or audit tables by a database account outside the application service account allowlist.
false_positives:
- Sanctioned admin or engineering activity
- Approved automation / CI workflows
tuning_guidance: Proposed rule generated from ATT&CK gap analysis — review the pseudo_logic, scope filters to the right asset group, and refine
  thresholds using ≥7 days of telemetry before promoting.
tuning_period: 14 days
tags:
- manipulation
- integrity
- t1565
- impact
references:
- https://attack.mitre.org/techniques/T1565/
author: TDL gap-analysis generator
created: '2026-04-27'
last_modified: '2026-07-18'
test_method: none
triage_steps:
- 'Confirm the alert metadata: timestamp, host(s) affected, scope of impact (count of files / accounts / services).'
- Inspect the calling identity (ARN / UPN), source IP, user agent, and the resource being acted on — characterize the destructive action (encrypt,
  delete, lockout, disable, reboot).
- 'Determine spread: is this a single host or a coordinated multi-host event? Pull file / service telemetry across the fleet.'
- Trigger your major-incident bridge if scope is multi-host or business-critical — early activation buys time.
- 'Decide TP / FP: FP only if a sanctioned destructive action (DR test, decommission) is documented; default to TP at this severity.'
- 'If TP: execute your IR runbook — isolate, preserve evidence, restore from backup if appropriate, hunt for adversary persistence to prevent
  recurrence.'

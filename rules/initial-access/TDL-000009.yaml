rule_id: TDL-AUTH-000009
v4_id: 000009
name: Successful Brute Force
description: Detects a successful login after the IP or host has been identified by password guessing. This could indicate that an attacker was
  successful in guessing a user’s password and has compromised their account.
tactic: Initial Access
tactic_id: TA0001
technique_id: T1078
technique_name: Valid Accounts
platform:
- Linux
- Windows
data_sources:
- Windows Active Directory
- Linux Operating System
- 'Event IDs: 4624'
severity: Medium
fidelity: Medium
lifecycle: Deployed
risk_score: 55
queries:
  spl: |
    index=wineventlog (EventCode=4625 OR EventCode=528 OR EventCode=540 OR EventCode=4624)
    | eval event_type=case(
        EventCode=4625, "failure",
        EventCode IN (528, 540, 4624), "success",
        1=1, "unknown"
    )
    | eval src_identifier=coalesce(IpAddress, Source_Network_Address, Workstation_Name)
    | eval dest_user=coalesce(TargetUserName, Account_Name, user)
    | eval dest_host=coalesce(ComputerName, dest, host)
    | sort 0 _time
    | streamstats time_window=3m count(eval(event_type="failure")) as failure_count by src_identifier, dest_user, dest_host
    | where failure_count >= 5
    | streamstats time_window=5m count(eval(event_type="success")) as success_count by src_identifier, dest_user, dest_host
    | where success_count >= 1 AND event_type="success"
    | dedup src_identifier, dest_user, dest_host
    | table _time, src_identifier, dest_user, dest_host, EventCode, failure_count, success_count
  kql: 'SecurityEvent

    | where TimeGenerated > ago(1d)

    | where event_id in ("4624")

    | summarize Count=count() by Computer, Account, IpAddress, LogonType, EventID

    | order by Count desc

    '
  aql: "SELECT DEVICETIME, sourceip, username, hostname, \"Logon Type\" as logon_type, eventid\nFROM events\nWHERE LOGSOURCETYPENAME(devicetype)\
    \ ILIKE '%Windows%'\n  AND \"event_id\" IN ('4624')\nLAST 24 HOURS\n"
  yara_l: "rule tdl_tdl_auth_000009 {\n  meta:\n    author = \"TDL\"\n    description = \"Successful Brute Force\"\n    severity = \"MEDIUM\"\n\
    \    tactic = \"TA0001\"\n    technique = \"T1078\"\n  events:\n    $e.metadata.event_type = \"USER_LOGIN\"\n    $e.event_id = /(4624)/\n\
    \    $user = $e.target.user.userid\n  match:\n    $user over 30m\n  condition:\n    $e\n}\n"
  esql: 'FROM logs-windows.security*,logs-system.security*

    | WHERE @timestamp > NOW() - 1 day

    | WHERE event.category == "authentication" AND event_id IN ("4624")

    | STATS count = COUNT(*) BY host.name, user.name, source.ip, winlog.event_data.LogonType, event.code

    | SORT count DESC

    '
  leql: 'where(logset IN ["Active Directory", "Asset Authentication"] AND event_id IN [4624])

    groupby(destination_account, source_ip, asset)

    calculate(count)

    '
  crowdstrike: 'event_simpleName IN (UserLogon, UserLogonFailed2, AuthenticationFailedRdp) AND event_id IN (4624)

    | stats count as auth_events by ComputerName, UserName, RemoteIP, LogonType

    | sort -auth_events

    '
  xql: 'dataset = xdr_data

    | filter event_type = ENUM.LOGIN AND event_id in ("4624")

    | comp count() as logins by agent_hostname, actor_effective_username, action_remote_ip, action_logon_type

    | sort desc logins

    '
  lucene: 'event.category:"authentication" AND event_id:(4624)

    '
  sumo: '_sourceCategory=*windows*security* OR _sourceCategory=*ad*

    | parse "EventID=*\"" as event_id nodrop

    | where event_id IN ("4624")

    | count by host, user, src_ip, logon_type, event_id

    | sort by _count desc

    '
false_positives:
- Legitimate admin activity
- Authorized service accounts
tuning_guidance: 'Tune per environment baseline. Tuning period: 7 days.'
tags:
- auth
- initial-access
author: TDL
created: '2024-01-01'
last_modified: '2026-07-18'
test_method: historical
pseudo_logic: "1 Password Guessing Event (5 failures within 3 minutes)\n\nFollowed By\n\n1 Windows Authentication Success Event\nWhere the Event\
  \ ID is in [528, 540, 4624]\nWith the same Source IP/Hostname\nWith the same Destination Username\nWith the same Destination Host\n\t\nWithin\
  \ 5 minutes"
suppression:
  window: 12 hours
  fields:
  - Source IP
  - Source Host
tuning_period: 7 days
triage_steps:
- 'Confirm the alert metadata: timestamp, target host/identity, source (IP, sender, URL), and the matched IOC.'
- Examine the source IP, geo / ASN, authentication method, and prior sign-in pattern for the user to assess whether the entry vector is consistent
  with this technique.
- Cross-reference with email gateway, web proxy, and EDR telemetry in the 30 minutes around the alert for related delivery activity.
- 'Pivot on the user / host: look for follow-on execution, persistence, or credential-access events in the next 24h.'
- 'Decide TP / FP / dual-use: confirm the user did not request the activity (sanctioned vendor onboarding, marketing campaign, pen test).'
- 'If TP: isolate the host, reset the user''s credentials and tokens, capture forensic artifacts, and open an incident ticket referencing this
  rule_id.'

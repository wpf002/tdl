rule_id: TDL-AUTH-000595
v4_id: 000595
name: HVT MFA Pin Guessing
description: This rule is designed to detect multiple failed MFA pin code attempts for the same high value target user against a company's MFA
  application. This may indicate an attempt to brute force a user's account and bypass MFA
tactic: Initial Access
tactic_id: TA0001
technique_id: T1078
technique_name: Valid Accounts
platform:
- Azure
- Okta
data_sources:
- Duo
- Okta
- Azure
severity: Medium
fidelity: Medium
lifecycle: Proposed
risk_score: 55
queries:
  spl: |
    (index=okta OR index=azure OR index=duo) (action=mfa_deny OR action=mfa_challenge_deny OR action=failure OR result=failure OR eventType=user.authentication.auth_via_mfa OR eventType=user.mfa.factor.update) (factor=pin OR factor=code OR factor="device code" OR factor_type=passcode OR factor_type=token OR authenticationMethod=passcode OR authenticationMethod=deviceCode)
    | eval user=coalesce(user, target.alternateId, userPrincipalName, username, dest_user)
    | eval src_ip=coalesce(src_ip, src, client.ipAddress, ipAddress, ip_address)
    | eval mfa_factor=coalesce(factor, factor_type, authenticationMethod, authentication_method)
    | search mfa_factor IN ("pin", "code", "device code", "passcode", "token", "deviceCode")
    | lookup high_value_users.csv user OUTPUT is_hvt
    | search is_hvt=1 OR is_hvt="true"
    | bin _time span=3m
    | stats count dc(user) as user_count values(user) as users values(mfa_factor) as factors by _time, src_ip
    | where count >= 5 AND user_count = 1
    | table _time, src_ip, users, factors, count
  kql: 'union AWSCloudTrail, AzureActivity, AuditLogs

    | where TimeGenerated > ago(1d)

    | where eventName matches regex ".+"

    | summarize Count=count() by Identity, EventName=coalesce(EventName, OperationName), Resource=tostring(coalesce(ResourceId, ResourceProvider))

    | order by Count desc

    '
  aql: "SELECT DEVICETIME, \"User Identity\", \"Event Name\", \"Source IP Address\"\nFROM events\nWHERE LOGSOURCETYPENAME(devicetype) IN ('AWS\
    \ CloudTrail','Azure Audit','GCP Cloud Audit')\n  AND \"eventName\" LIKE '%.+%'\nLAST 24 HOURS\n"
  yara_l: "rule tdl_tdl_auth_000595 {\n  meta:\n    author = \"TDL\"\n    description = \"HVT MFA Pin Guessing\"\n    severity = \"MEDIUM\"\n\
    \    tactic = \"TA0001\"\n    technique = \"T1078\"\n  events:\n    $e.metadata.event_type = \"USER_RESOURCE_ACCESS\"\n    $e.eventName =\
    \ /.+/\n    $actor = $e.principal.user.userid\n  match:\n    $actor over 30m\n  condition:\n    $e\n}\n"
  esql: 'FROM logs-aws*,logs-azure*,logs-gcp*

    | WHERE @timestamp > NOW() - 1 hour

    | WHERE eventName LIKE "%.+%"

    | STATS count = COUNT(*) BY user.name, event.action, cloud.account.id

    | SORT count DESC

    '
  leql: 'where(logset IN ["AWS CloudTrail", "Azure Audit", "GCP Audit"] AND eventName=/.+/)

    groupby(actor, event_name)

    calculate(count)

    '
  crowdstrike: 'event_simpleName=CloudOcsfApiActivity AND eventName=*.+*

    | stats count as calls by Actor, ApiName, SourceIPAddress

    | sort -calls

    '
  xql: 'dataset = cloud_audit_logs

    | filter eventName ~= ".+"

    | comp count() as calls by actor_effective_username, action_evtlog_event_name, action_remote_ip

    | sort desc calls

    '
  lucene: 'event.dataset:("aws.cloudtrail" OR "azure.audit" OR "gcp.audit") AND eventName:*.+*

    '
  sumo: '_sourceCategory=*aws/cloudtrail* OR _sourceCategory=*azure/audit* OR _sourceCategory=*gcp/audit*

    | where eventName matches "*.+*"

    | count by userIdentity_arn, eventName, sourceIPAddress

    | sort by _count desc

    '
false_positives:
- Legitimate admin activity
- Authorized service accounts
tuning_guidance: Tune per environment baseline.
tags:
- auth
- initial-access
author: TDL
created: '2024-01-01'
last_modified: '2026-07-18'
test_method: none
pseudo_logic: '5 MFA Login Failures

  From the same external  Source

  Where the MFA Factor is a Pin or Device Code

  Where the Destination Username is on the High Value Target User List

  With the same Destination Username

  Within 3 minutes'
triage_steps:
- 'Confirm the alert metadata: timestamp, target host/identity, source (IP, sender, URL), and the matched IOC.'
- Examine the calling identity (ARN / UPN), source IP, user agent, and the resource being acted on to assess whether the entry vector is consistent
  with this technique.
- Cross-reference with email gateway, web proxy, and EDR telemetry in the 30 minutes around the alert for related delivery activity.
- 'Pivot on the user / host: look for follow-on execution, persistence, or credential-access events in the next 24h.'
- 'Decide TP / FP / dual-use: confirm the user did not request the activity (sanctioned vendor onboarding, marketing campaign, pen test).'
- 'If TP: isolate the host, reset the user''s credentials and tokens, capture forensic artifacts, and open an incident ticket referencing this
  rule_id.'

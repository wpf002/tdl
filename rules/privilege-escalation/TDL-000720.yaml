rule_id: TDL-PE-000720
v4_id: '000720'
name: WMI Permanent Event Subscription Created
description: Creation of __EventFilter / __EventConsumer / __FilterToConsumerBinding in root\subscription. Classic stealth
  WMI persistence + privesc. Generated from gap analysis against canonical ATT&CK Enterprise matrix (T1546 under Privilege
  Escalation).
tactic: Privilege Escalation
tactic_id: TA0004
technique_id: T1546
technique_name: Event Triggered Execution
platform:
- Windows
- macOS
- Linux
data_sources:
- Sysmon
- EDR
- WMI Telemetry
severity: High
fidelity: High
lifecycle: Proposed
queries:
  spl: 'index=network sourcetype=firewall OR sourcetype=ids

    | search wmi.namespace="*wmi-sub*"

    | stats count, dc(dest_ip) as unique_dests, sum(bytes_out) as bytes_out by src_ip, dest_ip, dest_port

    | where count >= 1

    | sort - bytes_out'
  kql: 'union (DeviceNetworkEvents), (CommonSecurityLog | extend SourceIP=tostring(SourceIP), DestinationIP=tostring(DestinationIP))

    | where TimeGenerated > ago(1d)

    | where wmi.namespace matches regex "wmi-sub"

    | summarize Count=count(), Bytes=sum(coalesce(SentBytes, 0)) by SourceIP, DestinationIP, DestinationPort

    | order by Bytes desc'
  aql: 'SELECT sourceip, destinationip, destinationport, SUM(eventcount) as connections, SUM(bytessrc) as bytes_out

    FROM flows

    WHERE "wmi.namespace" LIKE ''%wmi-sub%''

    GROUP BY sourceip, destinationip, destinationport

    LAST 1 HOURS'
  yara_l: "rule tdl_tdl_pe_000720 {\n  meta:\n    author = \"TDL\"\n    description = \"WMI Permanent Event Subscription Created\"\
    \n    severity = \"HIGH\"\n    tactic = \"TA0004\"\n    technique = \"T1546\"\n  events:\n    $e.metadata.event_type =\
    \ \"NETWORK_CONNECTION\"\n    $e.wmi.namespace = /wmi-sub/\n    $src = $e.principal.ip\n  match:\n    $src over 10m\n\
    \  condition:\n    $e\n}"
  esql: 'FROM logs-network*,logs-endpoint*

    | WHERE @timestamp > NOW() - 1 hour

    | WHERE event.category == "network" AND wmi.namespace LIKE "%wmi-sub%"

    | STATS bytes_out = SUM(network.bytes), conns = COUNT(*) BY source.ip, destination.ip, destination.port

    | SORT bytes_out DESC'
  leql: 'where(logset = "Network Activity" AND wmi.namespace=/wmi-sub/)

    groupby(source_ip, destination_ip, destination_port)

    calculate(count, sum(bytes_out))'
  crowdstrike: 'event_simpleName IN (NetworkConnectIP4, NetworkConnectIP6, DnsRequest) AND wmi.namespace=*wmi-sub*

    | stats count as conns, sum(BytesSent) as bytes_out by ComputerName, RemoteAddressIP4, RemotePort

    | sort -bytes_out'
  xql: 'dataset = xdr_data

    | filter event_type = ENUM.NETWORK AND wmi.namespace ~= "wmi-sub"

    | comp count() as conns, sum(action_total_bytes) as bytes_out by agent_hostname, action_remote_ip, action_remote_port

    | sort desc bytes_out'
  lucene: event.category:"network" AND wmi.namespace:*wmi-sub*
  sumo: '_sourceCategory=*sysmon* OR _sourceCategory=*endpoint/edr* OR _sourceCategory=*windows/wmi*

    | parse "EventID=*\"" as event_id nodrop

    | where event_id IN ("12","13","14") AND _raw matches "*T1546*"

    | count by host, user, registry_key, registry_value, image

    | sort by _count desc'
pseudo_logic: Creation of __EventFilter / __EventConsumer / __FilterToConsumerBinding in root\subscription. Classic stealth
  WMI persistence + privesc.
false_positives:
- Sanctioned admin or engineering activity
- Approved automation / CI workflows
tuning_guidance: "Proposed rule generated from ATT&CK gap analysis \u2014 review the pseudo_logic, scope filters to the right\
  \ asset group, and refine thresholds using \u22657 days of telemetry before promoting."
tuning_period: 14 days
tags:
- wmi
- trigger
- subscription
- t1546
- privilege-escalation
references:
- https://attack.mitre.org/techniques/T1546/
author: TDL gap-analysis generator
created: '2026-04-27'
last_modified: '2026-04-27'
test_method: none

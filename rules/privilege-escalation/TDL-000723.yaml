rule_id: TDL-PE-000723
v4_id: '000723'
name: Scheduled Task Configured to Run as SYSTEM
description: schtasks /create /ru SYSTEM, or task XML RunLevel=HighestAvailable with UserId=NT AUTHORITY\SYSTEM. Scheduled escalation primitive.
  Generated from gap analysis against canonical ATT&CK Enterprise matrix (T1053 under Privilege Escalation).
tactic: Privilege Escalation
tactic_id: TA0004
technique_id: T1053
technique_name: Scheduled Task/Job
platform:
- Windows
- Linux
- macOS
data_sources:
- Sysmon
- Windows Event Log
- Auditd
severity: Medium
fidelity: High
lifecycle: Proposed
risk_score: 60
queries:
  spl: |
    index=wineventlog (sourcetype="WinEventLog:Microsoft-Windows-Sysmon/Operational" EventCode=1 Image="*\\schtasks.exe" (CommandLine="*/create*" AND CommandLine="*/ru*" AND (CommandLine="*SYSTEM*" OR CommandLine="*NT AUTHORITY\\SYSTEM*"))) OR (sourcetype=WinEventLog:Security EventCode=4698 Task_Content="*<RunLevel>HighestAvailable</RunLevel>*" Task_Content="*<UserId>NT AUTHORITY\\SYSTEM</UserId>*") OR (sourcetype=WinEventLog:Security EventCode=4698 Task_Content="*<RunLevel>HighestAvailable</RunLevel>*" Task_Content="*<UserId>SYSTEM</UserId>*")
    | table _time, ComputerName, User, Image, CommandLine, Task_Name, Task_Content
  kql: 'DeviceProcessEvents

    | where TimeGenerated > ago(1d)

    | where process_name matches regex ".+"

    | summarize Count=count() by DeviceName, AccountName, FileName, InitiatingProcessFileName, ProcessCommandLine

    | order by Count desc

    '
  aql: "SELECT DEVICETIME, sourceip, \"Process Name\", \"Parent Process Name\", \"Process CommandLine\"\nFROM events\nWHERE LOGSOURCETYPENAME(devicetype)\
    \ = 'Microsoft Windows Sysmon'\n  AND \"EventID\" = '1'\n  AND \"process_name\" LIKE '%.+%'\nLAST 24 HOURS\n"
  yara_l: "rule tdl_tdl_pe_000723 {\n  meta:\n    author = \"TDL\"\n    description = \"Scheduled Task Configured to Run as SYSTEM\"\n    severity\
    \ = \"MEDIUM\"\n    tactic = \"TA0004\"\n    technique = \"T1053\"\n  events:\n    $e.metadata.event_type = \"PROCESS_LAUNCH\"\n    $e.process_name\
    \ = /.+/\n    $host = $e.principal.hostname\n  match:\n    $host over 30m\n  condition:\n    $e\n}\n"
  esql: 'FROM logs-endpoint*,logs-windows*

    | WHERE @timestamp > NOW() - 1 hour

    | WHERE event.category == "process" AND process_name LIKE "%.+%"

    | STATS count = COUNT(*) BY host.name, user.name, process.name, process.parent.name, process.command_line

    | SORT count DESC

    '
  leql: 'where(logset = "Endpoint Activity" AND process_name=/.+/)

    groupby(asset, process_name, parent_process_name)

    calculate(count)

    '
  crowdstrike: 'event_simpleName=ProcessRollup2 AND process_name=*.+*

    | stats count as occurrences by ComputerName, UserName, FileName, ParentBaseFileName, CommandLine

    | sort -occurrences

    '
  xql: 'dataset = xdr_data

    | filter event_type = ENUM.PROCESS AND process_name ~= ".+"

    | comp count() as occurrences by agent_hostname, actor_effective_username, action_process_image_name, causality_actor_process_image_name,
    action_process_image_command_line

    | sort desc occurrences

    '
  lucene: 'event.category:"process" AND process_name:*.+*

    '
  sumo: '_sourceCategory=*sysmon* OR _sourceCategory=*endpoint*

    | parse "EventID=*\"" as event_id

    | where event_id="1" AND process_name matches "*.+*"

    | count by host, user, parent_process_name, command_line

    | sort by _count desc

    '
pseudo_logic: schtasks /create /ru SYSTEM, or task XML RunLevel=HighestAvailable with UserId=NT AUTHORITY\SYSTEM. Scheduled escalation primitive.
false_positives:
- Sanctioned admin or engineering activity
- Approved automation / CI workflows
tuning_guidance: Proposed rule generated from ATT&CK gap analysis — review the pseudo_logic, scope filters to the right asset group, and refine
  thresholds using ≥7 days of telemetry before promoting.
tuning_period: 14 days
tags:
- schtasks
- cron
- at
- t1053
- privilege-escalation
references:
- https://attack.mitre.org/techniques/T1053/
author: TDL gap-analysis generator
created: '2026-04-27'
last_modified: '2026-07-18'
test_method: none
triage_steps:
- 'Confirm the alert metadata: timestamp, host, source user, and (if visible) the resulting privileged context.'
- Inspect the parent → child process tree, command line arguments, and code-signing status — verify the privilege gain was not part of an approved
  elevation flow (UAC consent, sudo, PAM ticket).
- 'Pull preceding 1h of activity for the source user: was there earlier credential-access / discovery / exploit telemetry?'
- 'Validate token / session changes: new SeImpersonate-style privileges, new IAM role assumption, or a SYSTEM-level child process from a user-level
  parent.'
- 'Decide TP / FP: TP if there''s no change-management correlation and no parent admin process; FP if it''s a documented elevation by IT.'
- 'If TP: contain the host, revoke the elevated session, sweep for other escalations using the same primitive, and notify identity / IR teams.'
